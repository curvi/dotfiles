[user]
  name = niccnacc
  email = michael@schwegel.at
[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[alias]
  ci = commit
  co = checkout
  br = branch
  s  = status
	unstage = reset HEAD --
	noup  = log --graph --pretty=format:'%h - %s | %ar' --all @{upstream}^..
  list  = log --graph --pretty=format:'%h - %s | %ar ' --name-status -3
  list2 = log --graph --pretty=format:'%h - %s | %ar' --since=1.week
[color]
  ui = true
