# split current pane and run 'vim' command inside the new one
# panewidth 40cols, start dir like the original pane
splitw -h -p 40  -c "#{pane_current_path}" 'matlab -nodesktop -nosplash'
selectp -t 0 # select the first (0) pane

